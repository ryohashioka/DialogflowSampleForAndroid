# Dialogflow Java client library on Android sample

これは Dialogflow の Java client library を Andrid 上で動かすサンプルです。

# 手順
- put your gcp credentials ```credentials.json``` to ```res/raw/credentials.json```
